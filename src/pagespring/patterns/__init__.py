"""Source patterns.

Each module here implements the Pattern protocol (see pagespring/base.py) for one
source type — match(url) / acquire(url) / normalize(acq) / convert_recipe — and
registers itself in pagespring/registry.py.
"""
